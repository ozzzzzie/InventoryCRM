@model IEnumerable<NAIMS.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<style>
    .table-responsive.text-nowrap table {
        font-size: 14px;
    }
</style>

<h1>Local Inventory</h1>

<p align=right>
    <a asp-controller="Orders" asp-action="pickup" class="btn btn-dark">pick up items</a>
</p>

<div class="card">
    <div class="table-responsive text-nowrap">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["BarcodeSortParm"]">Barcode</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["SKUSortParm"]">SKU</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["BrandSortParm"]">Brand</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["ProdSortParm"]">Name</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["SizeSortParm"]">Size</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["LclSortParm"]">Local</a>
                    </th>
                    <th>
                        <a asp-action="LocalInventory" asp-route-sortOrder="@ViewData["LclStatSortParm"]">Status</a>
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Barcode)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Sku)
                        </td>
                        <td>
                            @* bug *@
                            @if (item.BrandId == 1)
                            {
                                <text>milk_shake</text>
                            }
                            else if (item.BrandId == 2)
                            {
                                <text>depot</text>
                            }
                            else if (item.BrandId == 3)
                            {
                                <text>simply zen</text>
                            }
                            else
                            {
                                @Html.DisplayFor(modelItem => item.BrandId)
                            }
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Pname)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Size)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.LocalQty)
                        </td>
                        <td>
                            @if (item.LocalStatus == "low stock")
                            {
                                <span class="badge rounded-pill bg-warning">@Html.DisplayFor(modelItem => item.LocalStatus)</span>
                            }
                            else if (item.LocalStatus == "pick up needed")
                            {
                                <span class="badge rounded-pill bg-danger">@Html.DisplayFor(modelItem => item.LocalStatus)</span>
                            }
                            else if (item.LocalStatus == "in stock")
                            {
                                <span class="badge rounded-pill bg-success">@Html.DisplayFor(modelItem => item.LocalStatus)</span>
                            }
                        </td>
                        <td>

                            <div class="dropdown">
                                <button type="button" class="btn p-0 dropdown-toggle hide-arrow" data-bs-toggle="dropdown"><i class="bx bx-dots-vertical-rounded"></i></button>
                                <div class="dropdown-menu">
                                    <a class="dropdown-item" asp-action="Edit" asp-route-id="@item.Barcode"><i class="bx bx-edit-alt me-1"></i> Edit</a>
                                    <a class="dropdown-item" asp-action="Details" asp-route-id="@item.Barcode"><i class="bx bx-info-circle"></i> Details</a>
                                    <a class="dropdown-item" asp-action="Delete" asp-route-id="@item.Barcode"><i class="bx bx-trash me-1"></i> Delete</a>
                                </div>
                            </div>


                        </td>
                    </tr>}
                            </tbody>
        </table>
    </div>
</div>

